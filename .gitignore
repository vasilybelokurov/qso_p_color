# Working notebook, deliberately local: it records machine state, timings and
# dead ends that are noise in a shared history. tools/journal.py maintains it.
JOURNAL.md

__pycache__/
*.egg-info/
.pytest_cache/
data/
models/*
# The model: one file, its reference-band priors, and its unmodelled term.
!models/multisurvey.json
!models/multisurvey_priors.json
!models/multisurvey_outlier.json
# Retired models, kept so earlier results and the model's own provenance
# (holdout blocks, prior normalisation) stay reproducible.
!models/archive/
*.npz
*.parquet

# macOS
.DS_Store

# External review reports (local working material, like JOURNAL.md)
docs/reviews/
