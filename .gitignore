# Working notebook, deliberately local: it records machine state, timings and
# dead ends that are noise in a shared history. tools/journal.py maintains it.
JOURNAL.md

__pycache__/
*.egg-info/
.pytest_cache/
data/
models/*
models/examples/
# ...except the trained quasar model, so the repo is usable without a 4-hour refit
!models/qso_south_full.json
# the other two ingredients the scorer needs, so a clone can score offline
!models/background_south_global.json
!models/background_density_south_global.json
!models/sigma_q_south.json
# Separate extension; the existing southern model remains available unchanged.
!models/multisurvey.json
# Preserve the initial joint-only extension for reproducible comparisons.
!models/multisurvey_joint_20260921.json
*.npz
*.parquet

# macOS
.DS_Store

# External review reports (local working material, like JOURNAL.md)
docs/reviews/
